---
import Layout from '../layouts/Layout.astro'
import ediciones from '../data/primeraera.json'
---
<Layout>
<div class="text-white text-center">
<h1 class="text-white text-6xl font-extrabold text-center leading-tight px-4">
Primera era</h1>
<h1 class="text-white text-2xl font-bold text-center leading-tight px-4">
Seras un honorable caballero, un feroz guerrero, un desalmado cazador <br /> 
o un poderoso dios? Solo tu puedes decidir.</h1>
</div>
  <!-- 🔹 Sección de tarjetas agregada -->
<div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6 justify-center mt-10">
{ediciones.map((item) => (
<div class="relative w-[475px] h-[633px] rounded-xl overflow-hidden shadow-lg group">
<img 
    src={item.imagen} 
    alt={item.nombre} 
class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"/> 
    <!-- Overlay negro al pasar por encima -->
<div class="absolute inset-0 bg-black opacity-0 group-hover:opacity-75 transition-opacity duration-300"></div>
    <!-- Contenido (nombre y descripción) -->
<div class="absolute bottom-0 left-0 right-0 flex flex-col items-center justify-center p-4 text-center text-white z-10">
<h3 class="font-semibold text-lg">{item.nombre}</h3>
<p class="text-sm opacity-90">{item.descripcion}</p>
</div>
</div>
))}
</div>
</Layout>
