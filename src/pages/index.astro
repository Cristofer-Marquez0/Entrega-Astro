---
import Layout from '../layouts/Layout.astro'
import tarjetas from '../data/tarjetas.json'
---
<Layout>
<div class="text-white text-center">
<h1 class="text-white text-6xl font-extrabold text-center leading-tight px-4">
Bienvenido Guerrero<br />¿listo para la batalla?
</h1>
<h1 class="text-white text-2xl font-bold text-center leading-tight px-4">
Elije tu bando y comienza tu aventura
</h1>
<section class="p-6">
<h2 class="text-2xl font-bold mb-4">Sección de Tarjetas</h2>
      <!-- Contiene el grid -->
<div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6 justify-center">
{tarjetas.map((item) => (
<a href={item.link} class="block">  <!-- Línea agregada: Enlace clickeable usando item.link -->
<div class="relative w-[343px] h-[343px] rounded-xl overflow-hidden shadow-lg group">
<img src={item.image} alt={item.name} 
class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"/> 
              <!-- Overlay negro al pasar por encima -->
<div class="absolute inset-0 bg-black opacity-0 group-hover:opacity-75 transition-opacity duration-300"></div>
              <!-- Contenido (nombre) -->
<div class="absolute bottom-0 left-0 right-0 flex items-center justify-center p-2 text-center font-semibold text-white z-10">
{item.name}
</div>
</div>
</a>  <!-- Cierre del enlace -->
))}
</div>
</section>
</div>
</Layout>